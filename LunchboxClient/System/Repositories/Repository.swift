/**
 A protocol used to ensure that an object can be stored in `Repositories`.
 Make sure that any time a new `Repository` is created, in order to use it, you must `register`
 It to the `Repositories` inside the DependencyContainer
 */

protocol Repository {
    
}
